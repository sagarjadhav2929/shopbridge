# shopbridge

# Download and extract zip file.
# Open terminal and go to extracted project folder and follow below steps

# Create and activate virtual environment
python3 -m venv venv
. ./venv/bin/activate

# Install Python dependencies
pip install -r requirements.txt

# Run Django dev server
./manage.py runserver

# Once project running download attached collection and refer to run api's
